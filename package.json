{
  "name": "onyx",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "services/*",
    "clients/ts",
    "app"
  ],
  "scripts": {
    "phase0": "bun run services/ingestion/src/phase0.ts",
    "auth": "bun run services/ingestion/src/auth.ts",
    "typecheck": "bunx tsc --noEmit",
    "demo": "bash scripts/run-demo.sh",
    "demo:amm": "cd app && bun scripts/amm_base_lifecycle.ts",
    "demo:amm-er": "cd app && bun scripts/amm_er_lifecycle.ts",
    "demo:session": "cd app && bun scripts/session_er_proof.ts"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
